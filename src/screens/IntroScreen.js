import { Text, View } from "react-native";

export default function IntroScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 24 }}>IntroScreen is working!</Text>
    </View>
  );
}
